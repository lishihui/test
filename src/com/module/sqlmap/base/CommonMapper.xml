<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
       "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.module.dao.base.CommonMapper">

	<insert id="insertSQL" parameterType="String">
		${value}
	</insert>
	
	<delete id="deleteSQL" parameterType="String">
		${value}
	</delete>
	
	<update id="updateSQL" parameterType="String">
		${value}
	</update>

	<select id="findOneData" parameterType="String" resultType="java.util.Map">
		${value}
	</select>

	<select id="findManyData" parameterType="String" resultType="java.util.Map">
		${value}
	</select>

	<select id="findCount" parameterType="String" resultType="long">
		${value}
	</select>

	<select id="findStringData" parameterType="String" resultType="String">
	 	${value}
	</select>
</mapper>